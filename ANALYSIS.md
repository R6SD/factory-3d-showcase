# 工厂 Web3D 工作台 — 项目调研分析报告

> 分析日期：2026-08-27
> 项目版本：1.0.0
> 分析范围：完整源码审查 + 架构诊断 + 优化建议

---

## 一、项目概览

| 维度 | 内容 |
|------|------|
| 项目名称 | 工厂 Web3D 工作台 (factory-3d-showcase) |
| 定位 | 纯前端工厂三维展示与企业工作台桌面应用 |
| 核心功能 | 3D 模型导入/展示、场景光影调节、悬浮标签标注、模型库管理、组织/人员/产能模拟数据 |
| 部署形态 | Electron 桌面端（安装版 + 便携版）+ 浏览器本地服务 |
| 作者 | Youhui Wen |

### 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 前端框架 | React | ^19.2.8 |
| 构建工具 | Vite | ^7.1.7 |
| 3D 引擎 | Three.js | ^0.180.0 |
| 路由 | react-router-dom | ^7.18.2 |
| 图标 | lucide-react | ^1.33.0 |
| 桌面壳 | Electron | ^37.2.6 |
| 打包 | electron-builder | ^26.0.12 |
| 语言 | JavaScript (JSX) | — |

---

## 二、架构分析

### 2.1 整体架构

```
┌─────────────────────────────────────────────────┐
│                   Electron 主进程                  │
│  electron/main.cjs                               │
│  ┌─────────────┐ ┌──────────┐ ┌───────────────┐ │
│  │ HTTP 服务器  │ │ 模型 API │ │ 标签 API      │ │
│  │ :43891      │ │ /models  │ │ /labels       │ │
│  └─────────────┘ └──────────┘ └───────────────┘ │
└──────────────────────┬──────────────────────────┘
                       │ 静态文件 + API
┌──────────────────────▼──────────────────────────┐
│                  浏览器渲染进程                     │
│                                                  │
│  ┌──────────────┐  ┌──────────────────────────┐ │
│  │ React 应用    │  │ 全局事件总线 (window)     │ │
│  │ src/app.jsx  │◄─►│ factory-import / show   │ │
│  │ (41KB 单文件) │  │ factory-scene-ready 等   │ │
│  └──────┬───────┘  └────────────┬─────────────┘ │
│         │                       │               │
│  ┌──────▼───────┐  ┌────────────▼─────────────┐ │
│  │ Three.js 场景 │  │ Vanilla JS 扩展 (public/) │ │
│  │ WebGL 渲染    │  │ 标签编辑器 / 模型存储 /   │ │
│  │              │  │ 场景持久化 / 服务控制     │ │
│  └──────────────┘  └──────────────────────────┘ │
└─────────────────────────────────────────────────┘
```

### 2.2 模块职责

| 文件 | 行数 | 职责 | 评价 |
|------|------|------|------|
| `src/app.jsx` | ~1200 | React 全部组件 + Three.js 场景 + 配置状态 | ⚠️ 过于集中 |
| `src/style.css` | ~290 | 基础布局与组件样式 | ✅ 结构清晰 |
| `src/overrides.css` | ~190 | 玻璃拟态、液态玻璃、悬浮标签等高级样式 | ✅ 视觉效果好 |
| `public/model-label-editor.js` | ~465 | 3D 悬浮标签（射线检测、SVG 标注线、拖拽） | ✅ 功能完整 |
| `public/model-library-storage.js` | ~56 | 模型文件存储（IndexedDB + 后端双写） | ✅ 降级合理 |
| `public/scene-persistence.js` | ~19 | 当前模型持久化与恢复 | ✅ 简洁 |
| `public/viewer-title-editor.js` | ~99 | 标题/品牌图标设置注入 | ⚠️ 依赖 DOM 注入 |
| `public/service-controls.js` | ~18 | 退出服务按钮注入 | ✅ 简洁 |
| `public/auto-dismiss-notices.js` | ~11 | 通知自动消失 | ✅ 简洁 |
| `electron/main.cjs` | ~118 | 本地 HTTP 服务 + 模型/标签 API | ✅ 安全到位 |

### 2.3 通信机制

项目采用 **全局 CustomEvent 事件总线** 模式，React 组件与 Vanilla JS 扩展通过 `window.dispatchEvent` / `window.addEventListener` 通信：

| 事件名 | 方向 | 用途 |
|--------|------|------|
| `factory-import` | 双向 | 导入模型文件 |
| `factory-show-model` | React→扩展 | 展示已保存模型 |
| `factory-show-default` | 双向 | 恢复默认场景 |
| `factory-scene-ready` | React→扩展 | Three.js 场景初始化完成 |
| `factory-active-model-change` | 扩展→React | 当前模型变更 |
| `factory-model-delete` | 扩展→React | 删除模型 |
| `factory-model-missing` | 扩展→React | 模型文件缺失 |
| `factory-viewer-title-change` | 扩展→React | 标题变更 |
| `factory-brand-icon-change` | 扩展→React | 品牌图标变更 |
| `factory-reset` | React→自身 | 重置视角 |

---

## 三、问题诊断

### 3.1 🔴 严重问题

#### P1: 单文件架构，可维护性差
`app.jsx` 41KB 包含 15+ 组件、Three.js 场景逻辑、配置管理、国际化字符串、模拟数据。所有内容挤在一个文件中，函数定义高度压缩（大量单行函数），阅读和修改困难。

#### P2: Three.js 持续渲染浪费性能
`Scene` 组件的 `loop()` 函数无条件 `requestAnimationFrame` 持续渲染，即使在：
- 场景静止（display=static）
- 标签无变化
- 页面不可见（切换到其他路由）
- 浏览器标签页后台

这导致桌面端持续占用 GPU/CPU，笔记本发热和耗电。

#### P3: 标签跟随循环持续运行
`model-label-editor.js` 的 `follow()` 函数无条件 `requestAnimationFrame`，即使当前模型没有任何标签也在运行。

### 3.2 🟡 中等问题

#### P4: 路由无代码分割
所有 7 个路由组件在首屏同步加载，包括设置页的实时 3D 预览（`ScenePreview`），增加了首屏 JS 体积和初始化时间。

#### P5: 头部元素被 CSS 隐藏但仍在 DOM
`overrides.css` 第 16 行用 `display:none!important` 隐藏了搜索框、通知、用户按钮，但这些元素仍在 `app.jsx` 中渲染，浪费渲染资源且代码混乱。

#### P6: 模型导入无加载反馈
大模型（最高 200MB）加载期间只有一行文字通知，无进度条、无加载遮罩，用户可能以为程序卡死。

#### P7: 无错误边界
Three.js 初始化失败、模型解析失败时，React 组件可能崩溃白屏，无降级 UI。

#### P8: 默认工厂场景过于简单
默认场景由几个 Box 和 Cylinder 拼凑，缺乏工业感（无传送带、管道、警示标识、环境细节），与"数字孪生"定位不匹配。

### 3.3 🟢 轻微问题

| 编号 | 问题 | 影响 |
|------|------|------|
| P9 | 无拖拽导入，只能点击按钮 | 体验不便 |
| P10 | 产能图表为纯 CSS 假柱状图，无动画 | 视觉粗糙 |
| P11 | 无空状态提示（模型库为空时） | 体验不完整 |
| P12 | 无键盘快捷键 | 效率低 |
| P13 | 最小宽度 1100px，无响应式适配 | 小屏不可用 |
| P14 | `viewer-title-editor.js` 读取 v3 存储键，与 v4 主存储不一致 | 潜在数据不同步 |
| P15 | 内联样式与 CSS 类混用 | 一致性差 |
| P16 | 无 WebGL 上下文丢失处理 | 极端情况崩溃 |

---

## 四、优化方案

### 4.1 性能优化（已实施）

1. **Three.js 自适应渲染**：场景静止时暂停渲染循环，仅在相机变化、动画播放、标签操作时渲染；页面不可见时完全暂停。
2. **路由懒加载**：使用 `React.lazy` + `Suspense` 分割路由组件，首屏只加载 3D 查看器。
3. **标签跟随优化**：无标签时暂停 rAF 循环；页面隐藏时暂停。
4. **GPU 资源管理**：模型切换时正确 dispose 旧模型几何体和材质。

### 4.2 视觉美化（已实施）

1. **增强默认工厂场景**：添加传送带、工业管道、储罐、警示柱、地面标线、环境雾效。
2. **氛围粒子**：3D 查看器添加悬浮微尘粒子，增强空间感。
3. **入场动画增强**：模型切换添加弹性缩放 + 位移动画 + 镜头脉冲。
4. **UI 微交互**：卡片悬浮抬升、按钮涟漪、图表入场动画、加载骨架屏。
5. **渐变与光晕**：标题渐变文字、状态指示灯、边角装饰线。

### 4.3 体验优化（已实施）

1. **拖拽导入**：支持将模型文件拖放到 3D 查看器区域导入。
2. **加载状态**：模型导入时显示加载遮罩和进度提示。
3. **键盘快捷键**：`R` 重置视角、`F` 全屏、`L` 切换标签。
4. **空状态**：模型库为空时显示引导提示。
5. **清理 DOM**：移除被 CSS 隐藏的头部元素。
6. **错误边界**：添加 React ErrorBoundary 防止白屏。

---

## 五、文件变更清单

| 文件 | 变更类型 | 说明 |
|------|----------|------|
| `src/app.jsx` | 修改 | 懒加载、自适应渲染、场景增强、拖拽导入、加载状态、快捷键、错误边界 |
| `src/style.css` | 修改 | 加载动画、拖拽指示、骨架屏、微交互样式 |
| `src/overrides.css` | 修改 | 粒子效果、增强过渡、空状态样式 |
| `public/model-label-editor.js` | 修改 | 标签跟随循环优化（无标签时暂停） |
| `ANALYSIS.md` | 新增 | 本分析报告 |

---

## 六、后续建议（未实施，供参考）

1. **组件拆分**：将 `app.jsx` 拆分为 `components/`、`scene/`、`data/`、`hooks/` 目录，提升可维护性。
2. **TypeScript 迁移**：添加类型安全，减少运行时错误。
3. **真实数据接入**：产能/人员/组织页面预留了 MES/IoT/HR 接口替换位，建议接入真实 API。
4. **模型格式扩展**：添加 STL、3MF 格式支持，添加 Draco 压缩模型解码。
5. **测量工具**：添加三维距离/角度测量功能。
6. **截图导出**：添加场景截图和高清渲染导出。
7. **多场景管理**：支持保存多个场景配置（相机位置、光照、标签）。
8. **协作标注**：标签支持评论、指派、状态流转。
