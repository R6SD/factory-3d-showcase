# 更新日志

本项目版本号遵循语义化版本（SemVer）：主版本.次版本.修订号；Windows 安装包另带四段式文件编号（末位为构建号）。

## [1.4.3] - 2026-09-01

### 新增（/admin 月度计划 Excel 导入/导出）
- **批量导入**：月度计划页新增「下载导入模板 / 导入 Excel」按钮，以 `月份`（yyyy-mm）、`计划产量` 两列批量导入计划并覆盖更新；非正数视为删除该月计划。
- **导出当前**：新增「导出当前」按钮，导出近 6 个月的月份、计划产量、实际产出三列。
- **纯函数扩展**：`src/data/admin-plans.js` 新增 `parsePlanImport`（解析 Excel 行为 `{ym,plan}`，过滤非法月份与非数字）、`mergePlanImport`（合并导入行，正数覆盖、0/空删除）、`buildPlanExportRows`（构建导出行），保持数据层纯函数风格。

### 变更
- `AdminPlans` 组件新增 `.admin-tools` 工具栏，与人员/产出记录页的导入导出能力对齐。

### 测试
- `tests/admin-plans.test.js` 由 5 组扩展为 9 组，覆盖 `parsePlanImport` / `mergePlanImport` / `buildPlanExportRows` 的正常/边界/脏输入；全量 **137 个** 单元测试通过。

### 构建
- 版本号 1.4.2 → 1.4.3，Windows 文件编号 1.4.3.0。

## [1.4.2] - 2026-09-01

### 新增（/admin 月度计划可视化编辑）
- **月度计划编辑页**：`/admin → 业务数据 → 月度计划` 新增第三个子标签，以表格形式展示近 6 个月（含当月）的月度计划总量，支持逐月直接修改计划产量并保存到 `business.monthlyPlans`；表格同时显示当月实际产出与达成率，达成率 ≥100% 绿色 / ≥90% 黄色 / <90% 红色，无计划时显示占位。
- **计划编辑纯函数**：新增 `src/data/admin-plans.js`，暴露 `buildPlanRows`（由记录与计划推导视图行）与 `applyPlanUpdates`（根据 draft 生成更新后的 plans，正数保留、非正数视为删除），保持数据层纯函数风格。
- **国际化**：`strings` 新增 `plansTitle/plansMonth/plansActual/plansPlan/plansAttainment/plansSave/plansSaved/plansHint` 中英文键。

### 变更
- `AdminBusiness` 子标签由 2 个扩展为 3 个（人员与组织 / 产出记录 / 月度计划），新增 `AdminPlans` 组件；`src/overrides.css` 补充 `.plan-table` 与 `.admin-hint` 样式。

### 测试
- 新增 `tests/admin-plans.test.js`（5 组用例），覆盖 `buildPlanRows` 与 `applyPlanUpdates` 的边界/脏输入/更新删除行为；全量 **133 个** 单元测试通过。

### 构建
- 版本号 1.4.1 → 1.4.2，Windows 文件编号 1.4.2.0。

## [1.4.1] - 2026-09-01

### 修复（后端数据管理 /admin 布局错位）
- **搜索栏双边框与撑爆标题栏**：`人员与组织` 的搜索框 `<input>` 此前无任何样式，落入浏览器默认样式，与 `.filter` 容器边框叠加成"框套框"，且无弹性收缩会把卡片标题栏撑爆错位；现补齐无边框透明输入、占位符与 `flex:1` 收缩，并给 `.filter` 限定弹性宽度区间。
- **记录表单字段塌缩**：`产出记录` 抽屉表单使用通用 `.form`，但 `.form label` 布局与 `.form input/select` 的 `width:100%` 缺失，导致输入控件宽度塌缩、字段上下不齐；现统一补齐，员工/记录两个抽屉表单的字段对齐一致。
- **表格窄屏溢出**：人员表（9 列）与产出记录表（7 列）此前无横向滚动容器，表头 `white-space:nowrap` 在窄窗口溢出主容器、元素错位；现为两张表套上 `.table-scroll` 横向滚动容器。
- **头部与主体左右不对齐**：`.admin-header` 此前全宽贴边、而 `.admin-main` 为 `max-width:1400px` 居中，超宽屏下头部导航与主体内容左右边缘错位；现头部改为内层 `.admin-header-inner` 同宽居中，品牌/导航靠左、返回按钮靠右。
- **编辑抽屉顶部空隙**：`.drawer` 沿用展示端 `top:74px` 固定定位，与 admin 头部实际高度不符，导致抽屉顶部出现空隙或被遮挡；现 admin 场景下抽屉改为顶到底全高（`top:0;height:100vh`）并提升层级。

### 变更
- `AdminApp` 头部引入 `.admin-header-inner` 内层容器；`AdminPeople` / `AdminRecords` 的表格外包 `.table-scroll`；`overrides.css` 补充 admin 搜索栏、表单字段、表格滚动与抽屉定位的修复样式。

### 测试
- 全量 128 个单元测试通过；`vite build` 生产构建成功。

### 构建
- 版本号 1.4.0 → 1.4.1，Windows 文件编号 1.4.1.0。

## [1.4.0] - 2026-09-01

### 新增（产能看板）
- **KPI 指标条**：看板顶部新增五张指标卡——本月总产出（含环比徽标）、日均产出、人均产出、计划达成率、重点关注（环比下滑部门数及名单），一屏掌握月度全局。
- **计划达成率**：业务数据新增 `monthlyPlans`（按月产量计划，种子按工作日 × 人员基准 × 目标系数确定性生成，可在 /admin 数据中整体替换），达成率分三档配色（≥100% 绿 / ≥90% 黄 / 其余红），无计划月显示占位。
- **近 6 个月产出趋势图**：按月总量的柱状趋势，点击柱体可直接切换查看月份；空月份显示占位。
- **部门下钻联动**：点击"整月部门总产能"任一部门行，分工段产出与个人产出排名即时过滤为该部门，面包屑一键返回全部部门；月份切换时自动重置下钻。
- **环比徽标**：部门/工段行内嵌与上月的环比百分比（涨红跌绿，国内配色习惯），上期无数据显示占位。
- **看板国际化**：产能看板全部文案接入中英文词表（`cap*` / `kpi*` 系列 40+ 键），英文月份 Tab 显示 Jan–Dec 缩写；此前看板为硬编码中文，切换语言不生效。
- **KPI 数字滚动**：五张指标卡数值接入项目自研 `NumberTicker` 缓动组件（v1.2.0），切换月份时数字平滑过渡，与场地总览页动效一致，并尊重系统"减少动态效果"偏好。
- 种子产出明细由"仅当月"扩展为**近 6 个月**（当月至今天、往月整月，跳过周末），并为每人每月叠加确定性"月度状态系数"，使趋势与环比有可解释的波动。

### 变更
- `selectors.js` 新增纯函数：`prevMonth`、`monthTrend`、`monthStats`、`monthPlanAttainment`、`groupSumWithDelta`、`recordsOfDept`；页面继续只消费选择器结果，聚合逻辑不出数据层。
- 产能看板组件重构：新增 `KpiCard` / `DeltaBadge` 子组件，原部门/工段/线体/排名四卡布局保留并增强。
- Electron 端已落盘的旧 `business.json` 保留原单月记录；在 `/admin` 重置数据或重新导入 Excel 后可获得 6 个月种子数据与月度计划。

### 测试
- 更新 `tests/data-layer.test.js`（近 6 个月覆盖、月度计划量级与合并、当月不含未来日期），`tests/capacity-selectors.test.js` 新增趋势/环比/下钻/达成率 6 组用例，全量 128 个单元测试通过。

### 构建
- 版本号 1.3.0 → 1.4.0，Windows 文件编号 1.4.0.0。

## [1.3.0] - 2026-08-29

### 性能优化
- **运动视差静止停绘**：旧逻辑下只要视差开关常开，即使指针静止、偏移已收敛为 0，仍逐帧提交 WebGL 渲染；现改为仅在视差阻尼未收敛、自动微摆或双目立体输出时连续渲染，指针静止并收敛后回到按需渲染。检视无动画模型等静态场景时空闲 GPU 不再空转、降低发热与功耗，移动鼠标即时恢复、跟手无损。
- **阴影按需重算**：关闭 `shadowMap.autoUpdate`，仅在场景确有动画，或切换模型、修改阴影质量等离散事件时重算 2048² 阴影贴图；轨道旋转、运动视差、纯静态查看不再每帧从光源视角把整场景额外重绘一遍。
- **静态展示冻结环境动画**：`display:'static'` 下冻结传送带滚轮/货箱与环境粒子，使画面真正静止并可完全空闲（dynamic / showcase 行为不变）。
- 渲染器请求高性能 GPU（`powerPreference:'high-performance'`），核显 + 独显双显卡设备优先使用独立 GPU。

### 新增
- **性能监控面板升级**：三维页 FPS 面板新增“渲染/秒、跳过/秒、GL 调用/帧、三角面/帧、立体模式”，数据取自 `renderer.info` 实测；原“绘制调用”为网格数近似值，现改为真实每帧 WebGL 绘制调用。
- 新增 `src/scene/render-policy.js`：帧渲染与阴影更新的纯函数判定模块，作为性能策略的单一事实源，便于单元测试与回归守护。

### 测试
- 新增 `tests/render-policy.test.js`（10 个用例，覆盖视差收敛、帧渲染判定矩阵、阴影按需），全量 100 个单元测试通过。

### 构建
- 版本号 1.2.0 → 1.3.0，Windows 文件编号 1.3.0.0。

## [1.2.0] - 2026-08-29

### 新增
- **零依赖界面动效组件库**：新增 `src/ui/`（`fx-utils.js` 缓动/补间/环形几何纯函数，`effects.jsx` 组件），参考 21st.dev / Magic UI 思路自实现，不引入 framer-motion、Tailwind 等任何第三方依赖。
- **KPI 数字滚动（NumberTicker）**：产能看板四个指标（今日生产、目标达成、OEE、设备在线）在加载、刷新或切换时由当前值平滑补间到目标值，等宽数字避免宽度抖动，并尊重系统“减少动态效果”偏好。
- **环形进度（RingProgress）**：纯 SVG 环形进度组件，用于产线下钻抽屉的达成率展示。
- **流星氛围背景（Meteors）**：三维查看页新增纯 CSS 流星氛围层，位于点阵纹理之上、标签与控件之下且不拦截交互；设置中心“模型与场景”新增开关，默认开启，旧配置经默认值合并自动获得。

### 变更
- 指标卡 `Metric` 新增 `num/fmt` 数值滚动用法，原有字符串用法完全兼容。
- 场景配置 `scene` 新增 `fxMeteors` 开关；中英文案同步补齐。

### 测试
- 新增 `tests/fx-utils.test.js`（8 个用例，覆盖数值夹取、缓动端点与单调性、补间边界、环形几何），全量 90 个单元测试通过。

### 构建
- 版本号 1.1.0 → 1.2.0，Windows 文件编号 1.2.0.0。

## [1.1.0] - 2026-08-29

### 新增
- **裸眼 3D 立体渲染**：新增 `src/scene/stereo.js`，提供运动视差、视差屏障、并排立体、红蓝立体五种模式。运动视差为默认模式，普通屏幕移动鼠标即可感知出屏纵深，无需眼镜；三维页操作栏新增“3D ·”快捷循环按钮，设置中心可切换模式、调节视差强度与自动微摆。
- **本地业务数据层**：新增 `src/data/`（种子数据、选择器、持久化仓储、React Provider）。桌面端经本地后端落盘到 `userData/business.json`，纯浏览器开发模式自动回落 localStorage，同一套异步 API，后续可整体替换为 MES/IoT/HR。
- Electron 本地服务新增 `GET/PUT /api/data/business` 业务数据读写接口，含体积上限（2 MB）与 JSON 合法性校验。
- 人员管理支持新增、编辑、删除，工号自动递增，表单校验，变更即时持久化。
- 产能看板新增“刷新数据”，按帧实时推演产量、OEE 与设备状态。
- 设置中心“数据与集成”显示当前数据来源、应用版本，并提供业务数据重置入口。

### 变更
- 产能、场地、人员、组织四个页面由前端写死数据改为数据层驱动；场地按层级关系自动生成树，组织关系按“直属上级”字段自动推导。
- 前端应用版本号改由 Vite 在构建期从 `package.json` 注入（`__APP_VERSION__`），界面版本不再写死。

### 测试
- 新增 `tests/stereo.test.js`、`tests/data-layer.test.js`，全量 82 个单元测试通过。

### 构建
- 版本号 1.0.0 → 1.1.0，Windows 文件编号 1.1.0.0。

## [1.0.0] - 2026-08
- 首个可用版本：三维模型导入与展示、悬浮标签、模型库、场景光影配置、产能/场地/人员/组织页面、Electron 桌面打包（NSIS 安装版与便携版）。
