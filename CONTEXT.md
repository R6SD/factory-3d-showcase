# 工厂 Web3D 场地总览

基于 Three.js 的工厂三维模型展示工作台，支持多模型切换、地图定位式标签、太阳光照调节和粒子背景。以 Electron 桌面应用形式分发，通过本地 HTTP 服务提供稳定的浏览器存储源。

## 术语

### 三维展示

**模型（Model）**:
在查看器中展示的单个三维对象，支持 GLTF/FBX/OBJ 格式。每个模型有独立的标签集和入场/退场动画。
_避免_：Object、mesh、asset

**标签（Label）**:
绑定到模型局部坐标的地图定位式标注，包含标题、描述和颜色。模型旋转时标签跟随锚点移动，不随视角消失。
_避免_：Annotation、marker、tooltip、hotspot

**场景（Scene）**:
Three.js 场景，包含当前模型、环境光、方向光（太阳）、粒子系统和地面网格。

**查看器（Viewer）**:
页面中央的三维渲染区域，承载场景、指南针、视图操作和悬浮标签层。

**指南针（Compass）**:
显示相机朝向的圆形表盘，N 指向模型正前方，可点击切换预设视角。

**太阳环绕（Sun Orbit）**:
方向光在模型上方的方位角/高度角调节，控制阴影方向和光照强度。

### 交互

**模型切换（Model Switching）**:
在已加载模型之间切换，带果冻弹簧动画的入场/退场效果。支持鼠标箭头、键盘左右键、触摸滑动三种方式。

**长按激活（Long-press Activation）**:
在模型上左键按下并保持 500ms 不移动，触发模型切换界面显示。激活时有按压回弹动画。

**视图预设（View Preset）**:
一键切换到正面、侧面、顶面或等轴测视角的快捷操作。

### 数据与存储

**模型库（Model Library）**:
已导入模型的持久化存储，优先使用后端文件系统，回退到 IndexedDB。模型名作为唯一键。

**产出记录（Output Records）**:
逐日、逐人、逐线体的产出明细（日期/部门/工段/线体/姓名/数量），是产能看板的唯一数据源；种子默认覆盖近 6 个月，可在 /admin 编辑或 Excel 导入。

**月度计划（Monthly Plans）**:
按月份（yyyy-mm）维护的产量计划总量，`business.monthlyPlans`；计划达成率 = 当月产出记录合计 / 月计划。种子按工作日 × 人员基准 × 目标系数确定性生成。

**后端数据管理（Admin /admin）**:
通过 `/admin` 路由进入的后台管理入口，集中维护业务数据、照片库、模型库与设置中心。业务数据分为「人员与组织」「产出记录」「月度计划」：人员与组织、产出记录支持 Excel 导入/导出、单条增删改；月度计划支持逐月直接编辑近 6 个月的计划产量。照片库按文件名规则自动匹配员工大头照与半身照。

**环比（MoM, Month-over-Month）**:
当前月份指标与上一月份同口径指标的百分比变化。看板中涨为红色、跌为绿色；上期无数据时显示占位。

**下钻（Drill-down）**:
产能看板中点击部门行，使工段产出与个人排名过滤为该部门的联动交互；月份切换或点击面包屑"全部部门"退出。

**场地总览（Site Overview）**:
应用的主页面，包含查看器、顶部标题栏、底部状态栏和侧边导航。
_避免_：Dashboard、workspace

**工作台配置（Workbench Config）**:
保存在 localStorage 的用户偏好，包括语言、主题、标题、品牌图标、太阳角度和显示设置。存储键为 `factory-workbench-v4`。
